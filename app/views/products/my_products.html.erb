<div class="container mt-4">
  <h2 class="title-h2">My Products</h2>
  <%= link_to 'Add Product', new_product_path, class: 'btn btn-custom mb-3' %>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Image</th>
        <th>Name</th>
        <th>Description</th>
        <th>Price</th>
        <th>Stock</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td>
            <% if product.image.attached? %>
              <%= image_tag product.image, size: "50x50", alt: product.name %>
            <% end %>
          </td>
          <td><%= product.name %></td>
          <td><%= product.description %></td>
          <td><%= number_to_currency(product.price) %></td>
          <td><%= product.stock %></td>
          <td>
            <%= link_to 'Edit', edit_product_path(product), class: 'btn btn-primary btn-sm' %>
            <%= button_to 'Delete', product_path(product, source: 'my_products'), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
